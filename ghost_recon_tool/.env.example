# Ghost Recon Tool — API Keys (optional, tool works without them)
# Get free keys to unlock more data sources
# Copy this file to .env and fill in your keys

GRT_GITHUB_TOKEN=        # github.com -> Settings -> Developer settings -> Personal access tokens
GRT_VIRUSTOTAL=          # virustotal.com -> Sign up free
GRT_OTX=                 # otx.alienvault.com -> Sign up free
GRT_URLSCAN=             # urlscan.io -> Sign up free
GRT_HUNTER_IO=           # hunter.io -> Sign up free (25/month)
GRT_ABUSEIPDB=           # abuseipdb.com -> Sign up free
GRT_SECURITYTRAILS=      # securitytrails.com -> Sign up (50/month free)
GRT_CENSYS_ID=           # censys.io -> Sign up (250/month free)
GRT_CENSYS_SECRET=       # censys.io -> Sign up (250/month free)
GRT_FULLHUNT=            # fullhunt.io -> Sign up free
GRT_HIBP=                # haveibeenpwned.com/API -> $3.50/month
GRT_INTELX=              # intelx.io -> Sign up free tier
GRT_BINARYEDGE=          # binaryedge.io -> paid
GRT_BEVIGIL=             # bevigil.com -> Sign up free
GRT_SHODAN=              # shodan.io -> paid
GRT_CHAOS=               # chaos.projectdiscovery.io -> Sign up free
GRT_BING_SEARCH=         # azure.microsoft.com -> Bing Search API
